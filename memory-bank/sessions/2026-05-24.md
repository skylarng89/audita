# Session Log — 2026-05-24

## Summary

Resolved web container build failures in CI/container scan path by restoring pnpm workspace policy parity inside Docker build layers, then cleaned deprecated pnpm config usage.

## Work Completed

### Container Build Reliability

- Investigated failing `web builder 5/7` install step (`ERR_PNPM_MINIMUM_RELEASE_AGE_VIOLATION`).
- Confirmed policy exclusions existed in `audita-web/pnpm-workspace.yaml` but file was not present during early Docker install layer.
- Updated `audita-web/Dockerfile` to copy `pnpm-workspace.yaml` alongside `package.json` and `pnpm-lock.yaml` before `pnpm install`.

### Docker Runtime/Builder Compatibility

- Adopted `dhi.io/node:24-dev` for builder stage to preserve shell/corepack tooling needed for install/build.
- Kept hardened runtime stage on `dhi.io/node:24` with numeric non-root user (`65532:65532`) and non-root file ownership in copy step.

### pnpm Configuration Cleanup

- Removed deprecated `package.json#pnpm` settings from `audita-web/package.json`.
- Moved `supportedArchitectures` policy to `audita-web/pnpm-workspace.yaml` (current pnpm-supported location).
- Verified deprecated warning noise removed during Docker build.

## Files Updated

- `audita-web/Dockerfile`
- `audita-web/package.json`
- `audita-web/pnpm-workspace.yaml`
- `memory-bank/context.md`
- `memory-bank/changelog.md`
- `memory-bank/decisions.md`
- `memory-bank/plan.md`
- `memory-bank/docs/tasks.md`
- `memory-bank/sessions/2026-05-24.md`

## Verification

- `docker build -t audita-web:scan -f audita-web/Dockerfile audita-web` ✅
  - Lockfile supply-chain policy check passed in container.
  - Nuxt production build completed and runtime artifact `.output/server/index.mjs` produced.

## Notes

- Remaining build output warnings are Vite/Tailwind sourcemap plugin notices; non-blocking and unrelated to policy/install failure.
