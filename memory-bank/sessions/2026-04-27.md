# Session Log — 2026-04-27

## Sprint 1: Authentication & Platform Bootstrap — COMPLETED

### What Was Done

1. **Compile fix — `TenantResolutionFilter` wrong module**
   - Problem: filter was in `infrastructure` which has no `jakarta.servlet` API
   - Fix: moved to `api/src/main/java/io/audita/api/security/TenantResolutionFilter.java`
   - Updated `SecurityConfig` import accordingly
   - Added `spring-boot-starter-web` to `infrastructure/build.gradle.kts` (needed by `SsoService` for `RestClient`)
   - `./gradlew :api:compileJava` → BUILD SUCCESSFUL

2. **AUTH-010 — 18 unit tests for `AuthService`** (`infrastructure/src/test/java/io/audita/infrastructure/service/AuthServiceTest.java`)
   - `@ExtendWith(MockitoExtension.class)` + real `BCryptPasswordEncoder(4)` via `ReflectionTestUtils`
   - `TenantContext.setCurrentTenant(TENANT)` in `@BeforeEach`, `TenantContext.clear()` in `@AfterEach`
   - Tests: login (success, wrong password, unknown user, suspended), domain whitelist (blocked/allowed), rate limiting (login 5/15min, forgot-password 3/hr), refresh token (rotation, revoked), reset password (success, expired), bootstrap (creates SA, already bootstrapped), logout (revokes, null no-op), SHA-256 (deterministic, collision-resistant)
   - Lesson: `assertThatThrownBy(...).extracting("errorCode")` fails for private fields — use `.extracting(e -> ((ExType) e).getErrorCode())` instead
   - `./gradlew :infrastructure:test` → BUILD SUCCESSFUL, 18/18 tests passing

3. **Memory bank updated** — context.md, tasks.md (Sprint 1 table added), changelog.md

### Outcome

Sprint 1: **22/22 tasks complete (100%)**. Ready for Sprint 2.

### Sprint 2 Scope (Next)

- TENANT-001 through TENANT-005: Tenant CRUD, provisioning (atomic: schema + migrations + admin + invite), domain whitelist mgmt, SSO config CRUD, integration tests
- USR-001 through USR-008: Seed built-in roles, user invite, accept-invite, CRUD, roles/permissions, Auditor enforcement, tests
- GRP-001/002: Group CRUD + member management
- PLATFORM-001 through PLATFORM-005: Platform dashboard UI, tenant management UI, provision new org, domain whitelist UI, SSO config UI
- Frontend: User Management, Roles & Permissions, Group Management pages
