# Session: 2026-06-15

## Focus
Bug fixes for new change request form + global loading overlay

## Changes

### Bug Fix: Department Dropdowns Always Empty
- **Root cause:** `UpsertDepartmentRequest.java` — primitive `boolean isActive` defaults to `false` when omitted from JSON
- **Fix:** Changed to `@NotNull Boolean isActive` in DTO; updated `DepartmentService.create()` and `update()` signatures

### Bug Fix: Missing Scope/Description Editor
- **Root cause:** `useEditor()` accidentally deleted in commit `a1580f8` when adding `activeDepartments` and `linkedRequestIds` refs
- **Fix:** Restored `useEditor()` initialization in `pages/change-requests/new.vue`
- **Side effect:** Also fixed "Unable to create change request" error (same root cause)

### Feature: Global Loading Overlay
- Replaced `<NuxtLoadingIndicator>` with full-screen centered overlay
- New files: `composables/useLoadingOverlay.ts`, `components/shared/SharedLoadingOverlay.vue`
- Modified `app.vue` with route watcher (200ms debounce)
- Integrated into all 13 data-fetching pages with `hideLoading()` on data resolve

### Polish: Department Empty-State Hint
- Added helpful message on new request form when no active departments exist

## Files Changed
- `audita-api/api/.../UpsertDepartmentRequest.java` — boolean → Boolean with @NotNull
- `audita-api/infrastructure/.../DepartmentService.java` — signature updates
- `audita-web/app.vue` — replaced NuxtLoadingIndicator with overlay + route watcher
- `audita-web/composables/useLoadingOverlay.ts` — new composable
- `audita-web/components/shared/SharedLoadingOverlay.vue` — new component
- `audita-web/pages/change-requests/new.vue` — restored editor, added department hint
- 13 data-fetching pages — loading overlay integration

## Verification Status
- Code changes applied — pending runtime verification
- No commits made (per user instruction)
