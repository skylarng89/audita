# Session: 2026-06-16

## Focus
Long-term fixes for 5 issues: loading overlay, CSP, SLA persistence, Groups/Departments, UAT blank tab

## Changes — By Issue

### A: Loading Overlay Race Condition
- **A.1** `useLoadingOverlay.ts` — replaced deferred `show()` with `showId` counter pattern; `triggerShow()` captures snapshot ID, 200ms later only shows if ID still matches; `hide()` increments counter to invalidate pending shows
- **A.1** `app.vue` — removed local `showTimer`, now calls `triggerShow()` from composable
- **A.2** 4 pages — added `hideLoading()` to catch/error blocks (`[id].vue`, `index.vue`, `dashboard/index.vue`, `admin/settings/index.vue`)

### B: CSP Style Violations
- `nuxt.config.ts` — added `style-src-elem: ['unsafe-inline']` and `style-src-attr: ['unsafe-inline']`

### C: SLA Persistence
- `TenantService.java` — replaced find-then-setValue with direct `save(new OrgSettingEntity(key, value))` upsert
- `admin/settings/index.vue` — re-reads SLA from PATCH response instead of trusting local state

### D: Groups/Departments Unification
- **D.1** `GroupEntity.java` — added `isActive`, `displayOrder` fields
- **D.1** `V9__unify_groups_departments.sql` — migration: add columns to groups, add FK columns to change_requests
- **D.2** `ChangeRequestEntity.java` — added `requestGroupId`, `destinationGroupId`
- **D.2** `CreateChangeRequestRequest.java`, `UpdateChangeRequestRequest.java`, `ChangeRequestResponse.java` — added group ID fields
- **D.2** `ChangeRequestService.java` — set group IDs in create/update
- **D.3** `types/index.ts` — added `requestGroupId`/`destinationGroupId` to `ChangeRequest`, `isActive`/`displayOrder` to `Group`
- **D.3** `useChangeRequests.ts` — added `listActiveGroups()`
- **D.3** `new.vue`, `[id].vue` — switched department dropdowns to use groups API
- **D.4** `CreateGroupRequest.java`, `GroupResponse.java` — added `isActive`, `displayOrder`
- **D.4** `GroupController.java` — added `?active=true` filter param
- **D.4** `GroupService.java` — added `listActiveGroups()`, updated `createGroup()`
- **D.4** `GroupRepository.java` — added `findAllByIsActiveTrue()`

### E: UAT Blank Tab
- **E.1** `CrUatPanel.vue` — `uat.approvers?.length` null-safety
- **E.2** `RequestUatResponse.java` — added `createdByFullName`, `approvers` fields; two `from()` factories
- **E.2** `RequestUatApproverResponse.java` — added `userFullName`, `userEmail`; dual `from()` factories
- **E.2** `RequestUatController.java` — updated get/create/update/promote to resolve users and approvers; added `GET /approvers` endpoint
- **E.2** `RequestUatService.java` — added `loadApproverUsers()`, `resolveUserFullName()`
- **E.2** `useChangeRequests.ts` — added `listUatApprovers()`
- **E.2** `CrUatPanel.vue` — updated `loadUat()` to fallback-fetch approvers

## Stats
- 28 files modified/created
- 271 insertions, 79 deletions
- No commits made (per user instruction)
