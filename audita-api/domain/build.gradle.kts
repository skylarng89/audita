// Domain module — pure business logic.
// No Spring dependencies; no infrastructure concerns.
// Depends on nothing within this project.
dependencies {
    implementation("jakarta.validation:jakarta.validation-api")
}
